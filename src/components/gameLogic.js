// export function findNearByCells(width, height, posX, posY) {
//     // top
//     const top = posY < 1 ? null : [posX, posY - 1]
//     const topLeft = posX < 1 ? null : ( posY - 1 < 0 ? null : [posX - 1, posY - 1] )
//     const topRight = posX < width - 1 ? (posY)
// }
